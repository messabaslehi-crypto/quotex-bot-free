services:
    - type: worker
    name: quotex-bot-free
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
            - key: TELEGRAM_TOKEN
        sync: false
