services:
    - type: web
    name: quotex-bot-free
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    autoDeploy: false
    envVars:
            - key: TELEGRAM_TOKEN
        sync: false
